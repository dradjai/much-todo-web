# TODO App - Full Stack App with User Login

  Used React with useContext hooks.  I couldn't implemented props because there was only 1 layer of sub-components, however I wanted to practice using Context.

